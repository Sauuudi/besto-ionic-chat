import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'besto-ionic-chat',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
